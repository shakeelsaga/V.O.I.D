====================================================================
  V.O.I.D. Observability Stack — Deployed Successfully
====================================================================

  Stack: Mosquitto → Telegraf → InfluxDB → Grafana

  GRAFANA UI:
    http://<external-ip>:{{ .Values.grafana.service.port }}
    Username: {{ .Values.grafana.adminUser }}
    Password: {{ .Values.grafana.adminPassword }}
    Dashboard: V.O.I.D. Survivor Telemetry (auto-provisioned)

  MQTT BROKER (Gateway Ingress):
    Host: <external-ip>
    Port: {{ .Values.mosquitto.service.port }}
    Configure SECRET_MQTT_BROKER_IP in gateway_node/secrets.h to match the Host IP.

  INFLUXDB UI:
    http://<node-ip>:{{ .Values.influxdb.service.port }} (ClusterIP only — port-forward to access)
    kubectl port-forward svc/{{ include "void-observability.fullname" . }}-influxdb {{ .Values.influxdb.service.port }}:{{ .Values.influxdb.service.port }} -n {{ .Release.Namespace }}

  VERIFY ALL PODS:
    kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }}

====================================================================
